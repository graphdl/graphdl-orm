API has endpoint URI .
Constraint Type has Constraint Type_code .
Constraint has Constraint_id .
Constraint has modality of Modality Type .
Constraint is involved in ConstraintSpan .
Constraint is of Constraint Type .
Constraint spans Role .
ConstraintSpan autofills from superset . For each ConstraintSpan , at most one of the following holds: that ConstraintSpan autofills from superset ; that ConstraintSpan ~autofills from superset .
ConstraintSpan ~autofills from superset . For each ConstraintSpan , at most one of the following holds: that ConstraintSpan autofills from superset ; that ConstraintSpan ~autofills from superset .
Each Alert is an instance of UI Element .
Each Button is an instance of Control .
Each Checkbox is an instance of Control .
Each Control is an instance of UI Element .
Each Date Picker is an instance of Control .
Each Graph Schema is an instance of Noun .
Each Graph is an instance of Resource .
Each Grid is an instance of UI Element .
Each Image is an instance of Control .
Each Label is an instance of Control .
Each Menu Button is an instance of UI Element .
Each Menu is an instance of UI Element .
Each Password Box is an instance of Control .
Each Search Box is an instance of UI Element .
Each Select List is an instance of Control .
Each Set Comparison Constraint is an instance of Constraint .
Each Slider is an instance of Control .
Each Status is an instance of Noun .
Each Text Area is an instance of Control .
Each Text Box is an instance of Control .
Each Time Picker is an instance of Control .
Each Toolbar Button is an instance of Toolbar Item .
Each Toolbar Item is an instance of UI Element .
Each Toolbar Separator is an instance of Toolbar Item .
Each Toolbar is an instance of UI Element .
Event Type can be created by Verb .
Event Type has Event Type_id .
Event Type has Name .
Event Type publishes to Stream .
Event has Event_id .
Event is created by Graph .
Event is created by State Machine .
Event is of Event Type .
Event occurred at Timestamp .
Graph Schema has Reading .
Graph Schema has Role .
Graph is done for now . For each Graph , at most one of the following holds: that Graph is done for now ; that Graph ~is done for now .
Graph is of Graph Schema .
Graph is referenced by Verb .
Graph uses Resource for Role .
Graph ~is done for now . For each Graph , at most one of the following holds: that Graph is done for now ; that Graph ~is done for now .
Guard Run has Guard Run_id .
Guard Run is run by Guard .
Guard Run references Graph .
Guard guards Transition .
Guard has Guard_id .
Guard references Graph Schema .
HTTP Operation has HTTP Operation_code . Role for HTTP Operation_code . The possible values of HTTP Operation_code in HTTP Operation has HTTP Operation_code are 'POST' , 'GET' , 'PUT' , 'PATCH' , 'DELETE' .
HTTP Operation is involved in API .
Language has Language_code .
Menu has Menu Button .
Noun has Noun_id .
Noun is described to AI by prompt Text .
Noun is displayed by UI Element .
Noun is of schema:Thing .
Noun plays Role .
Reading has Reading_id .
Reading has Text .
Reading is involved in ReadingIsUsedByVerb .
Reading is localized for Language .
Reading is used by Verb .
ReadingIsUsedByVerb is by HTTP Operation .
ReadingIsUsedByVerb is involved in API .
Resource has Resource_id .
Resource is of Noun .
Role has Role_id .
Role is involved in ConstraintSpan .
Role is used in Reading .
Schema:Thing has schema:Thing_Name .
Set Comparison Constraint has Argument Length .
State Machine Definition has State Machine Definition_id .
State Machine Definition is for Noun .
State Machine has State Machine_id .
State Machine is currently in Status .
State Machine is for Resource .
State Machine is instance of State Machine Definition .
Status is defined in State Machine Definition .
Stream has Name .
Stream has Stream_id .
Toolbar has Toolbar Item .
Transition has Transition_id .
Transition is from Status .
Transition is to Status .
Transition is triggered by Event Type .
UI Element has UI Element_id .
Verb has Name .
Verb has Verb_id .
Verb is involved in ReadingIsUsedByVerb .
Verb is performed during Transition (Mealy semantics) .
Verb is performed in Status (Moore semantics) .
