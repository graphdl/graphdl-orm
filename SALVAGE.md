# The salvage ledger

Quarry: canon-first @ 0fa14b7c, read-only (SPEC 14.1). One line per artifact:
source @ commit → disposition, gate evidence. Rejections are listed, never silent
(SPEC 14.3).

| source (quarry) | disposition | gate evidence |
|---|---|---|
| AREST.tex @ 0fa14b7c | adopted verbatim | the source itself (0.1) |
| engine/apps/redo-decision/readings/core.md @ 0fa14b7c | adopted as conformance app | G6 target; verdict ledger 181bc775 |
| engine/shared/arest.canon @ 0fa14b7c | adopted verbatim → /arest.canon | G1: 17 passed (362/345/17, boundary atoms ⊆ the 5 registered prims) |
| engine/python/kernel.py @ 0fa14b7c | adopted → host_py/kernel.py; one adaptation: `.reduce` alias import → direct self-reference in boundary() | G1 harness executes it; μ + BASE + defs store + Cor-6 boundary() |
| engine/python/canon.py @ 0fa14b7c | adopted → host_py/canon.py; one adaptation: shared() resolves the repo ROOT (canon at /arest.canon per D7/PLAN) | G1; binds θ₁ (Codd §2.2), never authors |
| engine/python/tromp.py @ 0fa14b7c | adopted → host_py/tromp.py; lazy imports relativized | it IS the G1 checker (tasks 20/22 lineage) |
| engine/python/__init__.py @ 0fa14b7c | REPLACED: minimal seed (kernel + alias table + canon); the quarry init boots engine/compiler/protocol/tools — Day-3 material, not salvaged blind | SPEC 14.2: host code enters on H-shape only |
| engine/tests/test_tromp.py @ 0fa14b7c | adopted → gates/g1_litmus.py; header swapped to host_py, assertions byte-identical | G1 green 2026-07-14, 17 passed in 5.82s |
| (rebuild addition) system:registered manifest DEF in /arest.canon | authored, not salvaged — SPEC 7.2/8.2: the five boundary prims declared ⟨name, dom, cod, origin⟩; dom/cod transcribed from the quarry impls (engine.py lex/implode/slug/escape_html/strip_prefix) | G1: 18 passed; census 363/346/17; manifest names == boundary atoms |
| engine/python/compiler.py @ 0fa14b7c | adopted → host_py/compiler.py; pyarest imports relativized | paper-order compiles 13/13 zero unparsed; G2 gates the behaviors Day 5 |
| engine/python/engine.py @ 0fa14b7c | adopted → host_py/engine.py (internals: cells, derive, violation expressions, boundary impls); the VERB surface is NOT the gate — SPEC 2.2 gate builds fresh on top | paper-order SM rows exact (from/to/initial); G1 still green |
| engine/python/tools.py @ 0fa14b7c | adopted → host_py/tools.py; pyarest imports relativized | imported by engine/compiler; optimizer + rust seam (Day-4 material) |
| engine/python/protocol.py persist section @ 0fa14b7c | SLICE transcribed → host_py/persist.py: symbol codec, save/load_sqlite, ingest_frozen; fingerprint ADAPTED (host sources + canon + grammar); seal branches dormant (NotImplementedError) | grammar freeze/thaw works; paper-order compile rides it |
| engine/shared/forml2-grammar.md @ 0fa14b7c | adopted → /forml2-grammar.md ("the grammar file is the parser") | ingested by grammar_D; G2 conformance-tests it Day 5 (SPEC 11.1) |
| (rebuild addition) apps/paper-order/readings/core.md | authored: the AREST.tex §1 listing verbatim (unwrapped) | compiles 13/13; G3 conformance app; trigger-reading resolution under investigation |
| (rebuild fix) _reading_synonym in host_py/compiler.py | authored: undeclared FT references resolve to the unique declared fact type sharing noun multiset + verb identity under regular inflections (candidate-set intersection, no stemmer); ambiguity stays unresolved | AREST.tex §1 normative example (trigger 'Customer places Order' ↔ declaration 'Order is placed by Customer'); G3 seed green, G1 unaffected; canon-lift candidate for the G2 sweep |
| (rebuild addition) gates/g3_paper_order.py | authored: the §1 listing compiles whole, triggers resolve across orientation, the machine assembles via system:sm_join, exactly-one → uc+mand cells | 22 passed with G1 |
| engine/shared/base/*.md @ 0fa14b7c (11 files) | adopted → readings/base/, scrub IN PROGRESS per 10.2 (validation.md prose fusion split: deontic sentence kept, explanation → comments) | G4 probe: fresh compile shows exactly TWO alethic self-violations (SMD initial statuses ×3; duplicate Format 'text') + ~15 unparsed (2 prose fusions, rest = NORMA forms → G2 backlog, restatable). The quarry's hundreds of violations were STALENESS of base.store.json — regenerate-don't-salvage vindicated |
| engine/python/protocol.py Registry.validate @ 0fa14b7c | TRANSCRIBED → host_py/gate.py sweep(): the absolute Def-6 sweep (validate_for per declared fact type over ⟨P,D⟩, modality flags); registered judges re-enter Day 6 | becomes THE ONE GATE's judgment leg (SPEC 2.2); gates/g4_base.py runs it |
| (rebuild fixes, G4 census v1→v6) host_py/gate.py + compiler.py | posture: the sweep follows the STORE's declaration (rmapColumns present ⇒ partition, else own-table — raw compile_model stores are own-table by design; view members over never-laid rows read past row ends to ⊥, the 21-checker str family AND the 239s whales); M bridge with native-priority population fetch; D2 defaults (_default_noun_facts: every declared noun gets 'open' world assumption + object type, recorded, declared wins); fail-closed sweep; settle before judgment | census: 21 str + whales → 0 str, sweep 354s→~100s own-table; alethic 6→4 (WA + Object_Type cleared); the four remaining = the 10.1 self-description closure, pinned in the gate, Day 5 |
| readings/base scrubs (10.2) | validation.md fusion split; core.md ring notes ×2, catalog preamble, #279 paragraphs, JSON-projection block commented; csdp.md preamble commented; 6 restatements (5 external-UCs → spanning_uc form, data-type opt-in → instance fact) | unparsed 32→21, ALL prose out; the 21 = pinned G2 grammar backlog in gates/g4_base.py |
| (rebuild) apps/{organizations,redo-decision}/readings | staged from quarry templates + engine/apps | G3/G6 conformance set complete in-tree |
| (rebuild) host_py/gate.py envelope: compile_serving (compile→status_facts→layout_cells→settle), step(D, ops, journal) with §6.1-scoped judgment (_scoped_fts/sweep_scoped), _retract_row (D1/2.5: log-cell removal, derived heads cleared, supersession discipline cited) | authored per SPEC 2.1–2.5/§12; gates/g5_gate.py 4/5: batch swap valid-to-valid in ONE step (2.4 no-wedge, the old world's impossible move), lone orphaning retract REFUSED, journal committed-only, Def-5 unchanged-D on refusal | OPEN (pinned red in G5): the exclusion family's compiled checkers judge the participation join on the WRONG columns — shared noun Option is π₁ in eliminated, π₂ in uses; both clauses join π₁ → empty intersect → forbidden write passes. Task-25 class; likely never enforced end-to-end in the quarry either |
| (rebuild fix, G5 5/5) canon + compiler: the exclusion family's PROJECTED participation | constraints:scoped_exclusion_projected_many joins the canon (the union of position-projected pairs, data-built via the established apndl/CONST(CONS) idiom); _h_set_comparison resolves each clause's fact type AND the subject's role position (_clause_ft_roles); _cook_cs emits the @clause objects from the projected builder; _rebuilt's exclusion clobber retired (log cells are population truth on both postures) | G5 5/5: the forbidden write REFUSED at the door; census 364/347; full suite 29 passed (G1+G3+G4+G5). The family likely never enforced end-to-end before: the quarry's create never consulted it AND its checkers joined column 1 of every clause |
| (rebuild) gate.Journal + gate.replay (§12) | authored: durable jsonl, committed inputs only, arrival order; replay folds through THE GATE and HALTS loud on an entry that no longer commits | G5 7/7 incl. replay-reproduces + forbidden-entry-halts (the anti-thesis of the quarry's immortal resurrection, 181bc775) |
| engine/rust @ 0fa14b7c (src, build.rs, Cargo.*, tests; no target/) | adopted → host_rs/; adaptations: include! paths ../../shared/ → ../../ (canon at root per D7), build.rs rerun-if-changed likewise | builds clean 14m49s (warnings only) — the REBUILT canon (manifest + projected-many) parses as Rust; G3 cases leg GREEN: --cases vs the Python reducer, every scenario byte-identical |
| engine/shared/scenarios.canon @ 0fa14b7c | adopted verbatim → /scenarios.canon (the cross-host case table) | consumed by both hosts in g3_cases.py |
| engine/tests/test_csharp_kernel.py harness @ 0fa14b7c | _show + cases-differential TRANSCRIBED → gates/g3_cases.py (rust flavor) | 1 passed 8.92s |
| (rebuild) host_rs walkers adapted + /forml2-grammar.store.json exported | base_seed_paths + load_grammar_scratch walk for the ROOT layout (grammar sidecar beside the canon; readings/base; base.store.json at root, runtime-regenerated); the sidecar exported from host_py per main.rs's own documented recipe (grammar_D + compiled defs.latest, 213KB/118 cells/372 defs) — regenerated artifact, G2 re-derives Day 5 | native suite 19/19: serve_ops 16 (incl. overrides ⊆ catalog — the 7.3 shadowing discipline, exercising the NATIVE base compile over the scrubbed readings), derive 1, native_apply 2 |
