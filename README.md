# AREST — rebuild

The canon-first rebuild of AREST against the whitepaper. The constitution is
[SPEC.md](SPEC.md); the paper is [AREST.tex](AREST.tex); the salvage ledger is
SALVAGE.md. The quarry is the `canon-first` branch, frozen at 0fa14b7c.

The shared codebase is the polyglot λ file `arest.canon` (SPEC 8.1, D7); hosts are
thin μ-evaluators (SPEC §9). Green means SPEC §13's seven gates. `main` is replaced
by this branch only on Samuel's explicit word (SPEC D5).
